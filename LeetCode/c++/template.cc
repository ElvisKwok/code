#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    Type funcName(vector<int>& nums, ... ) {

    }
};

int main(int argc, char **argv)
{
    if (argc < 2)
        return 0;
    Solution s;

    return 0;
}
